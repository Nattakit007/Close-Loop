# คู่มือจูนค่า (Tuning Guide)

เขียนสำหรับคนที่เพิ่งเริ่ม — อธิบายทีละขั้น ไม่ต้องมีพื้นฐานเรื่องระบบควบคุมมาก่อน

---

## ⚠️ อ่านก่อนเริ่ม

**ค่าที่อยู่ในโค้ดตอนนี้เป็นค่าสมมติทั้งหมด** ผมใส่ไว้ให้โปรแกรม compile ผ่านและมีจุดเริ่มต้นให้ปรับ
แต่**ยังไม่ได้วัดจากเครื่องจริงของคุณ** ค่าที่ต้องจูนคือ:

| ค่า | อยู่ที่ | ตอนนี้ตั้งไว้ | ความหมาย |
|---|---|---|---|
| `DEG_PER_MM` | [main.c](Core/Src/main.c) | 3.6 | หมุนกี่องศา สายไฟถึงเดิน 1 มม. |
| `BLADE_CUT_DEG` | [main.c](Core/Src/main.c) | 180.0 | ใบมีดลงเท่าไหร่ถึงตัดขาด |
| `BLADE_STRIP_DEG` | [main.c](Core/Src/main.c) | 120.0 | ใบมีดลงเท่าไหร่ถึงปอกฉนวนพอดี |
| `STRIP_PULL_MM` | [main.c](Core/Src/main.c) | 8.0 | ป้อนสายเข้ามากี่มม. ก่อนปอกหัว |

**ห้ามให้ใบมีดทำงานใกล้สายไฟก่อนจูนค่าเสร็จ** ใบมีดที่ลงลึกเกินจะชนแท่นหรือหักได้

---

## ขั้นที่ 0: ทดสอบสวิตช์ก่อน

เครื่องนี้มีสวิตช์ตัวเดียว คือ **สวิตช์ใบมีด (PA10)** ต้องแน่ใจว่าอ่านค่าได้ถูก ไม่งั้นการ home จะพัง

> roller ไม่มีสวิตช์และไม่ต้อง home เพราะเป็นล้อป้อนสาย หมุนอย่างเดียว

**วิธีทำ** — เพิ่มโค้ดนี้ชั่วคราวใน `main()` ก่อนเข้า `while(1)`:

```c
/* ทดสอบสวิตช์ใบมีด - ลบออกเมื่อทดสอบเสร็จ */
while (1) {
    char buf[48];
    snprintf(buf, sizeof(buf), "{\"blade_sw\":%d}\n",
             HAL_GPIO_ReadPin(Blade_Limit_GPIO_Port, Blade_Limit_Pin));
    Send_ESP32_Msg(buf);
    HAL_Delay(200);
}
```

**สิ่งที่ควรเห็น:**
- ตอนไม่กดสวิตช์ → `1` (HIGH)
- ตอนกดสวิตช์ → `0` (LOW)

**ถ้าค่าไม่เปลี่ยนเลย** แปลว่าสายขาดหรือต่อผิด — ตรวจสายก่อนไปต่อ
**ถ้าค่ากลับกัน** (ไม่กด = 0) แปลว่าต่อสวิตช์เข้า VCC แทน GND — ย้ายมาลง GND

---

## ขั้นที่ 1: หา `DEG_PER_MM` (สำคัญที่สุด)

ค่านี้บอกว่า **มอเตอร์หมุนกี่องศา สายไฟถึงเดินไป 1 มิลลิเมตร**
ถ้าค่านี้ผิด ความยาวสายที่ตัดจะผิดหมดทุกเส้น

### วิธีคำนวณจากขนาด roller (แม่นพอสำหรับเริ่มต้น)

```
DEG_PER_MM = 360 ÷ (π × เส้นผ่านศูนย์กลาง roller เป็น มม.)
```

**ตัวอย่าง** roller ขนาด 32 มม.:
```
เส้นรอบวง = 3.1416 × 32 = 100.5 มม.
หมุน 1 รอบ (360°) สายเดิน 100.5 มม.
DEG_PER_MM = 360 ÷ 100.5 = 3.58
```

> ค่า 3.6 ที่อยู่ในโค้ดตอนนี้ตรงกับ roller ขนาดประมาณ 31.8 มม.

### วิธีวัดจริง (แม่นกว่า แนะนำให้ทำ)

1. ทำเครื่องหมายบนสายไฟตรงจุดที่ออกจากเครื่อง
2. สั่งงาน 1 ชิ้น ตั้งความยาว **100 มม.**
3. วัดระยะจริงที่สายเดินไปด้วยไม้บรรทัด
4. คำนวณ:

```
DEG_PER_MM ใหม่ = DEG_PER_MM เดิม × (100 ÷ ระยะที่วัดได้จริง)
```

**ตัวอย่าง** ตั้งไว้ 3.6 สั่ง 100 มม. แต่วัดได้จริง 95 มม.:
```
ค่าใหม่ = 3.6 × (100 ÷ 95) = 3.79
```

ทำซ้ำ 2-3 รอบจนได้ค่าที่คลาดเคลื่อนน้อยกว่า 1 มม.

---

## ขั้นที่ 2: หาระยะใบมีด

### เตรียมตัว

1. **เอาสายไฟออกจากเครื่องก่อน**
2. เพิ่มโค้ดทดสอบชั่วคราวใน `main()`:

```c
/* ทดสอบใบมีด - ลบออกเมื่อจูนเสร็จ */
Home_Blade();                    /* ใบมีดขึ้นบนสุด ตั้งเป็น 0 */
HAL_Delay(1000);

Blade_MoveTo(30.0f);             /* ลองลง 30 องศา */
HAL_Delay(3000);                 /* ดูว่าลงไปแค่ไหน */

Blade_MoveTo(0.0f);              /* กลับขึ้น */
while(1);                        /* หยุดตรงนี้ */
```

3. เปลี่ยนเลข `30.0f` ทีละนิด (30 → 60 → 90 → ...) แล้วดูว่าใบมีดลงไปแค่ไหน

### หาค่า `BLADE_STRIP_DEG` (ปอกฉนวน)

ปอกฉนวนคือ **กรีดแค่เปลือกพลาสติก ไม่โดนทองแดง**

1. ใส่สายไฟเข้าไป (ยังไม่ต้องเปิดระบบอัตโนมัติ)
2. ค่อยๆ เพิ่มองศาทีละ **10 องศา** แล้วดึงสายออกมาดู
3. **จุดที่ใช่**: ฉนวนขาดรอบเส้น แต่ดึงออกแล้วทองแดงไม่มีรอย
4. **ลึกเกิน**: ทองแดงเป็นรอยหรือขาดเป็นเส้นๆ → ลดลง 5-10 องศา
5. **ตื้นเกิน**: ฉนวนไม่ขาด ดึงไม่ออก → เพิ่ม 10 องศา

จดค่าที่ได้ แล้วเอาไปใส่ใน `BLADE_STRIP_DEG`

### หาค่า `BLADE_CUT_DEG` (ตัดขาด)

1. เริ่มจากค่า `BLADE_STRIP_DEG` แล้วเพิ่มทีละ **20 องศา**
2. **จุดที่ใช่**: สายขาดสนิทเป็นสองท่อน หน้าตัดเรียบ
3. **อย่าให้ลึกเกินจำเป็น** เพราะใบมีดจะชนแท่นรอง สึกเร็ว หรือหัก

**เผื่อความปลอดภัย**: เมื่อได้ค่าที่ตัดขาดพอดีแล้ว ให้บวกเพิ่มอีก **5-10 องศา** เท่านั้น ไม่ต้องมากกว่านี้

---

## ขั้นที่ 3: จูน PID (ทำทีหลัง ถ้าจำเป็น)

**PID คืออะไร (อธิบายแบบง่าย)**

PID คือสูตรที่ทำให้มอเตอร์ "วิ่งไปหยุดตรงจุดที่ต้องการ" โดยไม่แกว่งไปมา มี 3 ตัวคูณ:

| ตัว | ชื่อ | ทำหน้าที่ | ถ้ามากไป | ถ้าน้อยไป |
|---|---|---|---|---|
| **Kp** | Proportional | ยิ่งห่างเป้า ยิ่งเร่งแรง | แกว่งไปมารอบเป้า | ช้า ไปไม่ถึงเป้า |
| **Ki** | Integral | แก้ความคลาดเคลื่อนที่ค้างอยู่ | เลยเป้า (overshoot) | หยุดก่อนถึงเป้าเล็กน้อย |
| **Kd** | Derivative | เบรกก่อนถึงเป้า | สั่น เสียงดัง | เลยเป้าแล้วค่อยถอย |

ค่าปัจจุบันในโค้ด: `Kp=15.0, Ki=0.5, Kd=0.08` (ทั้ง roller และใบมีด)

### จูนเมื่อไหร่?

จูนต่อเมื่อเจออาการเหล่านี้:

| อาการที่เห็น | แก้ยังไง |
|---|---|
| มอเตอร์สั่นตอนหยุด ไม่นิ่ง | **ลด Kp** ลง 20% (15 → 12) |
| ช้ามาก กว่าจะถึงเป้า | **เพิ่ม Kp** ขึ้น 20% (15 → 18) |
| วิ่งเลยเป้าแล้วถอยกลับ | **เพิ่ม Kd** (0.08 → 0.12) |
| หยุดก่อนถึงเป้านิดหน่อยทุกครั้ง | **เพิ่ม Ki** (0.5 → 0.8) |
| แกว่งไปมาไม่หยุด | **ลด Ki** (0.5 → 0.2) และ **ลด Kp** |
| มีเสียงหวีดแหลม | **ลด Kd** (0.08 → 0.04) |

### ลำดับที่แนะนำ

1. ตั้ง `Ki = 0` และ `Kd = 0` ก่อน
2. เพิ่ม `Kp` จนมอเตอร์เริ่มแกว่ง แล้วลดลงมาครึ่งหนึ่ง
3. เพิ่ม `Kd` ทีละนิดจนไม่เลยเป้า
4. เพิ่ม `Ki` ทีละนิดจนหยุดตรงเป้าพอดี

แก้ค่าได้ที่ [main.c](Core/Src/main.c) บรรทัดที่มี `PID_Init`:

```c
PID_Init(&pid_roller, 15.0f, 0.5f, 0.08f, -20000.0f, 20000.0f);
/*                     Kp     Ki     Kd                        */
```

> **ใบมีดกับ roller จูนแยกกันได้** เพราะภาระต่างกัน ใบมีดมักต้องการ `Kp` สูงกว่าเพราะต้องออกแรงกด

---

## ขั้นที่ 3.5: เรื่องความเร็ว (สำคัญ — อ่านด้วย)

ผมรันจำลองการทำงานด้วยค่า PID ปัจจุบัน (`Kp=15`) แล้วพบว่า:

| งาน | เวลาที่ใช้ |
|---|---|
| ป้อน 8 มม. | ~1.9 วินาที |
| ป้อน 250 มม. | ~2.4 วินาที |
| ใบมีดลง+ขึ้น 1 ครั้ง | ~4.5 วินาที |
| **รวม 1 ชิ้น** | **~11 วินาที** |

**สาเหตุ**: `Kp=15` หมายความว่ามอเตอร์จะวิ่งเต็มความเร็ว (7500 Hz) ก็ต่อเมื่อ
ห่างจากเป้าหมายถึง **500 องศา (139 มม.)** งานส่วนใหญ่สั้นกว่านั้น มอเตอร์เลยวิ่งช้ากว่าที่ทำได้จริง

### ถ้าอยากให้เร็วขึ้น

เพิ่ม `Kp` — ผมทดสอบแล้วได้ผลดังนี้:

| Kp | ป้อน 8 มม. | ป้อน 100 มม. | หมายเหตุ |
|---|---|---|---|
| 15 (ปัจจุบัน) | 1907 ms | 2353 ms | ปลอดภัย ช้า |
| 50 | 645 ms | 1060 ms | **เร็วขึ้น 3 เท่า** |
| 100 | 325 ms | 525 ms | เร็วขึ้น 5 เท่า |

**ผมไม่ได้เปลี่ยนค่าให้** เพราะการเพิ่ม `Kp` บนเครื่องจริงต้องดูว่ามอเตอร์
**ตกขั้น (skip step)** หรือไม่ ซึ่งจำลองไม่ได้ ขึ้นกับแรงบิดมอเตอร์และภาระจริง

**วิธีทดลอง**: เพิ่ม `Kp` ทีละ 10 (15 → 25 → 35 → ...) แล้วดูว่า
- ความยาวยังตรงอยู่ไหม (ถ้าเพี้ยน = ตกขั้น → ถอยกลับ)
- มีเสียงกึกๆ ผิดปกติไหม
- ใบมีดยังหยุดตรงตำแหน่งไหม

> **ข้อควรระวังกับใบมีด**: `Kp` สูงทำให้ใบมีดลงเร็วและแรง ถ้าลงเลยเป้า
> อาจชนแท่นรองจนใบหัก แนะนำให้ใบมีดใช้ `Kp` ต่ำกว่า roller

---

## ขั้นที่ 4: ค่าอื่นที่ปรับได้

| ค่า | ตอนนี้ | ปรับเมื่อ |
|---|---|---|
| `STEP_FREQ_MAX` | 7500 | ลดลงถ้ามอเตอร์ตกขั้น (เสียงกึกๆ แล้วไม่หมุน) |
| `STEP_FREQ_HOMING` | 2000 | ลดลงถ้าชนสวิตช์แรงเกินไป |
| `POS_TOLERANCE_DEG` | 0.75 | **อย่าตั้งต่ำกว่า `PID_DEADBAND_DEG`** ไม่งั้นจะรอไม่มีวันจบ |
| `MOVE_TIMEOUT_BASE_MS` | 3000 | เวลาเผื่อคงที่ (timeout จะบวกตามระยะทางให้เองอัตโนมัติ) |
| `PID_DEADBAND_DEG` | 0.70 | ระยะที่ถือว่า "ถึงแล้ว" — คำนวณจาก `STEP_FREQ_MIN ÷ Kp` |
| `BLADE_SETTLE_MS` | 150 | เพิ่มถ้าตัดไม่ขาดสนิท (ให้ใบมีดค้างนานขึ้น) |
| `STRIP_PULL_MM` | 8.0 | ตั้งตามความยาวที่ต้องการปอกหัวสาย |

---

## ลำดับการทำงานของเครื่อง (เอาไว้อ้างอิง)

```
รับคำสั่งจาก ESP32
   ↓
HOMING_BLADE   ← ใบมีดขึ้นบนสุดจนชนสวิตช์ PA10 → ตั้งเป็น 0
   ↓            (roller ไม่ต้อง home — นับ 0 จากตอนเปิดเครื่อง)
┌─ FEEDING     ← ป้อนสายเข้า 8 มม. (STRIP_PULL_MM)
│     ↓
│  STRIP_HEAD  ← ใบมีดลง 120° ปอกหัว แล้วถอยขึ้น
│     ↓
│  (ป้อนต่อ)   ← ป้อนตามความยาวชิ้นงาน
│     ↓
│  STRIP_TAIL  ← ใบมีดลง 120° ปอกท้าย แล้วถอยขึ้น
│     ↓
│  CUTTING     ← ใบมีดลง 180° ตัดขาด แล้วถอยขึ้น
│     ↓
│  นับชิ้น +1 → ส่ง progress ให้ ESP32
└─────┘ วนกลับไปทำชิ้นถัดไปจนครบจำนวน
   ↓
IDLE           ← ส่ง {"done":true}
```

---

## เมื่อมีปัญหา

| อาการ | สาเหตุที่เป็นไปได้ |
|---|---|
| เครื่องค้างตอน home ใบมีด | สวิตช์ PA10 ต่อผิดหรือสายขาด (มี timeout 15 วิ แล้วขึ้น error) |
| ใบมีดวิ่งลงตอน home แทนที่จะขึ้น | ทิศกลับด้าน — สลับสายมอเตอร์คู่ใดคู่หนึ่ง **รีบปิดเครื่องก่อนใบชน** |
| ขึ้น error "motion fault" | มอเตอร์ไปไม่ถึงเป้าใน 5 วินาที — สายติด หรือกระแส driver ต่ำเกิน |
| ความยาวไม่ตรง | `DEG_PER_MM` ผิด — กลับไปทำขั้นที่ 1 |
| ความยาวเพี้ยนเพิ่มขึ้นเรื่อยๆ ทีละชิ้น | roller ลื่น — เพิ่มแรงบีบ roller หรือลด `STEP_FREQ_MAX` |
| ใบมีดไม่ขยับ | driver ตัวที่ 3 address ผิด (ดู [HARDWARE.md](HARDWARE.md) หัวข้อ 3) |
| ตัดไม่ขาด | เพิ่ม `BLADE_CUT_DEG` หรือ `BLADE_SETTLE_MS` |
| ปอกแล้วทองแดงขาด | ลด `BLADE_STRIP_DEG` |
