# Hardware Setup & Wiring

คู่มือการต่อสายและตั้งค่าฮาร์ดแวร์สำหรับเครื่องป้อน-ปอก-ตัดสายไฟ

---

## 1. ภาพรวมเครื่อง

เครื่องนี้ทำงาน 3 อย่างกับสายไฟเส้นเดียว: **ป้อน** (feed) → **ปอกฉนวน** (strip) → **ตัด** (cut)

```
        [ม้วนสายไฟ]
             |
             v
   ┌─────────────────┐
   │  M1 Roller หน้า │  ← มี encoder (MT6816 #1) → ควบคุมด้วย PID
   └─────────────────┘
             |
             v
      ╔═════════════╗
      ║  M3 ใบมีด   ║  ← มี encoder (MT6816 #2) → ควบคุมด้วย PID
      ║  (ขึ้น-ลง)  ║     + limit switch ด้านบน
      ╚═════════════╝
             |
             v
   ┌─────────────────┐
   │  M2 Roller หลัง │  ← ไม่มี encoder (หมุนตาม M1)
   └─────────────────┘
             |
             v
      [สายไฟที่เสร็จแล้ว]
```

---

## 2. ตารางขา (Pin Map)

### ขาที่ใช้อยู่เดิม

| ขา | ชื่อ | หน้าที่ |
|---|---|---|
| PA0 | STEP | สัญญาณพัลส์ขับสเต็ปเปอร์ (**แชร์ทั้ง 3 ตัว**) |
| PA1 | DIR | ทิศทางหมุน (**แชร์ทั้ง 3 ตัว**) |
| PA2 | EN1 | เปิด/ปิด driver M1 (active LOW) |
| PB0 | EN2 | เปิด/ปิด driver M2 (active LOW) |
| PB1 | EN3 | เปิด/ปิด driver M3 (active LOW) |
| PA3 | CS2 | Chip select ของ encoder ใบมีด |
| PA4 | CS1 | Chip select ของ encoder roller |
| PA5 | SPI1_SCK | สัญญาณนาฬิกา SPI |
| PA6 | SPI1_MISO | ข้อมูลจาก encoder เข้า MCU |
| PA7 | SPI1_MOSI | ข้อมูลจาก MCU ไป encoder |
| PA9 | USART1_TX | สายเดี่ยวคุย TMC2209 ทั้ง 3 ตัว |
| PA10 | Limit Switch | สวิตช์ home ของ roller |
| PA11 | USART6_TX | ส่งข้อมูลไป ESP32 |
| PA12 | USART6_RX | รับข้อมูลจาก ESP32 |
| PB2 | CS3 | (สำรอง ยังไม่ได้ใช้) |

### ⚠️ ขาที่ต้องเดินสายเพิ่ม

| ขา | ชื่อ | หน้าที่ |
|---|---|---|
| **PB4** | **Blade Limit** | **สวิตช์ home ของใบมีด — ต้องต่อเพิ่ม** |

**วิธีต่อ:**

```
   สวิตช์ใบมีด (ติดที่จุดบนสุดของราง)
        ┌───┐
  PB4 ──┤   ├── GND
        └───┘
```

- ต่อขาหนึ่งของสวิตช์เข้า **PB4** อีกขาลง **GND**
- **ไม่ต้องใส่ตัวต้านทาน** เพราะโค้ดเปิด internal pull-up ไว้แล้ว
- ตอนสวิตช์ **ยังไม่ชน** = อ่านได้ HIGH
- ตอนใบมีด **ชนสวิตช์** = อ่านได้ LOW (เรียกว่า active LOW เหมือนสวิตช์ตัวเดิม)
- ใช้สวิตช์แบบ NO (Normally Open) — ปกติวงจรเปิด กดแล้วต่อ

> **ทำไมต้องมีสวิตช์ตัวที่สอง?**
> สวิตช์ PA10 ที่มีอยู่ใช้หา "จุดศูนย์" ของ roller ซึ่งอยู่ตรงทางเข้าสายไฟ ส่วนใบมีดอยู่คนละที่กัน จึงต้องมีสวิตช์ของตัวเอง ไม่งั้นระบบจะไม่รู้ว่าใบมีดอยู่สูงแค่ไหน

---

## 3. การตั้ง Address ของ TMC2209

driver ทั้ง 3 ตัวใช้สาย UART **เส้นเดียวกัน** (PA9) จึงต้องมีเลขประจำตัวไม่ซ้ำกัน
ตั้งด้วยการต่อขา **MS1** และ **MS2** บนตัว driver:

| Driver | มอเตอร์ | Address | MS1 | MS2 |
|---|---|---|---|---|
| ตัวที่ 1 | M1 Roller หน้า | 0 | GND | GND |
| ตัวที่ 2 | M2 Roller หลัง | 1 | VCC | GND |
| ตัวที่ 3 | M3 ใบมีด | 2 | GND | VCC |

> ถ้าตั้งซ้ำกัน driver จะตอบพร้อมกันแล้วชนกัน มอเตอร์จะทำงานผิดหรือไม่ทำงานเลย

---

## 4. เรื่องสำคัญ: ทำไม M1 กับ M2 หมุนพร้อมกันได้

บอร์ดนี้มีสาย STEP แค่เส้นเดียว (PA0) ต่อเข้า driver **ทั้ง 3 ตัวพร้อมกัน**
การเลือกว่าตัวไหนจะขยับ ใช้ขา EN เป็นตัวคุม

โค้ดใช้หลักการนี้:

```
เวลาป้อนสาย  → เปิด EN1 + EN2 พร้อมกัน, ปิด EN3
                → M1 กับ M2 รับพัลส์ชุดเดียวกัน หมุนพร้อมกัน ทิศเดียวกัน

เวลาตัด/ปอก  → ปิด EN1 + EN2, เปิด EN3
                → เฉพาะใบมีดที่ขยับ
```

**ข้อดี:** ไม่ต้องเดินสาย STEP เพิ่ม, roller สองตัวหมุนตรงกันเป๊ะโดยธรรมชาติ

**ข้อจำกัดที่ต้องรู้:**
- roller ทั้งสองต้องตั้ง microstep เท่ากัน (โค้ดตั้ง 1/16 ให้ทั้งคู่แล้ว)
- ต้องใช้ roller ขนาดเส้นผ่านศูนย์กลางเท่ากัน ไม่งั้นสายจะยืดหรือย่น
- ระบบวัด encoder จาก M1 ตัวเดียว ถ้า M2 ลื่นไถล ระบบจะไม่รู้
- **ป้อนสายกับตัดพร้อมกันไม่ได้** ต้องทำทีละอย่าง (ซึ่งเป็นลำดับปกติของเครื่องอยู่แล้ว)

---

## 5. ทิศทางการหมุน

โค้ดกำหนดไว้ว่า:

| แกน | DIR = LOW (RESET) | DIR = HIGH (SET) |
|---|---|---|
| Roller | ป้อนสายไป**ข้างหน้า** | ดึงสาย**ถอยหลัง** |
| ใบมีด | เลื่อน**ลง** (ตัด) | เลื่อน**ขึ้น** (ถอย) |

**ถ้าทดสอบแล้วหมุนกลับทาง** ให้แก้อย่างใดอย่างหนึ่ง:
1. สลับสายคู่ใดคู่หนึ่งของมอเตอร์ (เช่น A+ กับ A−) — ง่ายที่สุด
2. หรือกลับ logic ในโค้ดที่ [main.c](Core/Src/main.c) ฟังก์ชัน `Control_Loop_1kHz()`

---

## 6. Checklist ก่อนเปิดเครื่องครั้งแรก

- [ ] ต่อสวิตช์ใบมีดเข้า PB4 และ GND แล้ว
- [ ] ตั้ง MS1/MS2 ของ driver ทั้ง 3 ตัวให้ address ไม่ซ้ำ
- [ ] **ถอดใบมีดออกก่อน** หรือเอาสายไฟออกจากเครื่อง (กันความเสียหายตอนทดสอบ)
- [ ] ตรวจว่ากระแส driver ไม่สูงเกินสเปคมอเตอร์
- [ ] ทดสอบว่าสวิตช์ทั้งสองตัวอ่านค่าถูก (ดูใน [TUNING.md](TUNING.md))
- [ ] จูนค่า `BLADE_CUT_DEG` และ `BLADE_STRIP_DEG` ตาม [TUNING.md](TUNING.md)

> ⚠️ **ค่าระยะใบมีดในโค้ดตอนนี้เป็นค่าสมมติ** ยังไม่ได้วัดจากเครื่องจริง
> **ห้ามให้ใบมีดทำงานใกล้สายไฟก่อนจูนค่าเสร็จ**
