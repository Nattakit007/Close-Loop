# Changelog

สรุปทุกอย่างที่แก้ในโปรเจกต์นี้ ตั้งแต่เริ่มรีวิว PID จนถึงเปิดใช้งานมอเตอร์ครบ 3 ตัว
เรียงตามไฟล์ ไม่เรียงตาม commit เพื่อให้เห็นภาพรวมว่าแต่ละไฟล์เปลี่ยนไปจากเดิมยังไงบ้าง

---

## ภาพรวม

| ก่อนแก้ | หลังแก้ |
|---|---|
| มีแค่ M1 (roller หน้า) ทำงานจริง | M1 + M2 (roller คู่) + M3 (ใบมีด) ทำงานครบ |
| `STATE_CUTTING` เป็น `HAL_Delay(300)` ไม่ตัดจริง | มีลำดับ ป้อน→ปอกหัว→ป้อน→ปอกท้าย→ตัด ครบ |
| PID มี anti-windup ที่ไม่ทำงานจริง | anti-windup แบบ back-calculation ใช้งานได้ |
| มอเตอร์ไหลตอนจอด (ยังไม่รู้ตอนนั้น) | ไหล 2°/0.5s → เหลือ 0.04°/0.5s |
| `DEG_PER_MM = 3.6` (ค่าเดา) | `DEG_PER_MM = 3.8197` (คำนวณจาก roller 30 มม. จริง) |
| ทิศใบมีดกลับด้าน (จะพุ่งชนแท่นตอน home) | แก้ทิศถูกแล้ว |
| PA10 เป็นขาลอย (floating) | เปิด internal pull-up |
| ไม่เคย build จริง | build ผ่านด้วย ARM GCC 14.2, ไม่มี warning |

---

## Core/Src/PID.c และ Core/Inc/PID.h

**ปัญหาที่เจอ:**
- Anti-windup ไม่ทำงานจริง — integral ยังโตต่อได้แม้ output ชนเพดานแล้ว ทำให้ overshoot ตอนวิ่งระยะไกล
- Derivative หาจาก error ตรงๆ ทำให้เกิด spike 2 แบบ: ตอนสั่ง target ใหม่ (setpoint kick) และตอน deadband ตัด error เป็น 0 ทันที
- `integral_limit` clamp ผิดตัวแปร — clamp ที่ `integral` ดิบ ไม่ใช่ที่ `Ki × integral` ทำให้เพดานจริงไม่ตรงกับชื่อตัวแปร
- deadband (0.05°) แคบกว่าโซนที่ step generator ไม่ตอบสนอง (0.67°) ทำให้ integral สะสมจนดันมอเตอร์กระตุกเป็นช่วงๆ ตอนควรจะนิ่ง

**แก้เป็น:**
- Anti-windup แบบ back-calculation: เมื่อ output saturate จะถอย `integral` กลับตามส่วนที่เกิน
- เปลี่ยนเป็น derivative-on-measurement (diff จาก feedback แทน error) ไม่มี spike จาก setpoint หรือ deadband
- เพิ่มธง `d_initialized` กันไม่ให้ derivative รอบแรกหลัง reset เพี้ยน (เพราะ `prev_feedback` ยังเป็น 0)
- `integral_limit` clamp ที่ `i_out` แล้วย้อนคำนวณ `integral` กลับให้ตรงกัน
- เพิ่ม `PID_SetTarget()` — ล้าง integral/derivative ตอนสั่งเป้าใหม่ โดยไม่แตะ `accumulated_angle_deg` (ไม่งั้น multi-turn tracking จะหาย)
- deadband ในโค้ดตอนนี้ยังล้าง `integral` เป็น 0 ด้วยเมื่ออยู่ในโซนนั้น (แก้ที่ main.c เป็นคนตั้งค่าให้เหมาะสม — ดูหัวข้อถัดไป)

---

## Core/Src/main.c (ไฟล์หลักที่แก้เยอะที่สุด)

### 1. เปิดใช้งานมอเตอร์ครบ 3 ตัว

- `motor_3` ประกาศไว้แต่ไม่เคย `TMC2209_Init()` มาก่อน → เพิ่ม init ให้ address 2
- เปลี่ยนชื่อตัวแปรจาก `encoder_2`/`pid_2` เป็น `encoder_blade`/`pid_blade` ให้สื่อความหมายตรงกับฮาร์ดแวร์จริง (encoder ตัวที่ 2 อยู่ที่ใบมีด ไม่ใช่ roller หลัง)
- เพิ่ม `Select_Drive_Target(DRIVE_ROLLERS / DRIVE_BLADE / DRIVE_NONE)` แทนฟังก์ชันเดิม `Select_Active_Motor()` — เปิด EN1+EN2 พร้อมกันสำหรับ roller คู่ (หมุนตรงกันเพราะรับ STEP/DIR เส้นเดียวกัน), เปิด EN3 อย่างเดียวสำหรับใบมีด

### 2. State machine ใหม่ทั้งหมด

ของเดิม: `IDLE → HOMING → FEEDING → CUTTING(delay 300ms) → วนกลับ FEEDING`

ของใหม่: `IDLE → HOMING_BLADE → FEEDING → STRIP_HEAD → (ป้อนต่อ) → STRIP_TAIL → CUTTING → วนกลับ FEEDING`

- `STATE_HOMING` (ของ roller) **ถูกลบออก** — เพราะ roller เป็น feeder หมุนอย่างเดียว ไม่มีจุดอ้างอิง ตัวนับเริ่มที่ 0 ตอนเปิดเครื่อง (seed ครั้งเดียวใน `main()`) แล้วนับสะสมต่อเนื่องข้ามชิ้นงาน
- `STATE_HOMING_BLADE` เป็นการ home จุดเดียวที่เครื่องนี้มี — ใบมีดถอยขึ้นจนชนสวิตช์ (PA10) แล้วตั้งเป็น 0 ทำทุกครั้งที่เริ่มงานใหม่ เพื่อให้ระยะตัด/ปอกแม่นยำซ้ำได้
- `STATE_CUTTING` เดิมเป็น placeholder (`HAL_Delay(300)` แล้วนับชิ้น) ตอนนี้สั่งใบมีดลงจริงตามระยะ (`BLADE_CUT_DEG`) ค้างไว้ (`BLADE_SETTLE_MS`) แล้วถอยขึ้น

### 3. ฟังก์ชันควบคุมการเคลื่อนที่ (ของใหม่ทั้งหมด)

| ฟังก์ชัน | หน้าที่ |
|---|---|
| `Control_Loop_1kHz()` | เรียกจาก TIM1 ทุก 1 ms อ่าน encoder ของแกนที่ active อยู่ รัน PID แปลงเป็นความถี่ step |
| `Step_SetFrequency()` / `Step_Stop()` | คุม PWM แบบ start/stop เฉพาะตอนเปลี่ยนสถานะจริง (ของเดิมเรียก start ทุก 1 kHz ทำให้ step pulse ขาด) |
| `Wait_For_Move()` | บล็อกจนกว่าจะถึงเป้า, abort, error, หรือ timeout (timeout คำนวณจากระยะทาง ไม่ใช่ค่าคงที่) |
| `Home_Blade()` | ถอยใบมีดขึ้นแบบ open-loop จนชนสวิตช์ แล้ว set 0 มี timeout 15 วิ กันค้างถาวร |
| `Feed_Wire_mm()` | สั่งป้อนสายเป็นระยะสัมพัทธ์ (มม.) แบบ closed-loop |
| `Blade_Stroke()` | สั่งใบมีดลงถึงความลึกที่กำหนด ค้าง แล้วถอยขึ้น ใช้ร่วมกันทั้งปอกและตัด |

### 4. แก้บั๊กในลูปควบคุม

- **PWM restart ทุก 1 kHz** — `HAL_TIM_PWM_Start()` ถูกเรียกซ้ำทุกรอบทำให้ step pulse ขาดเป็นช่วงๆ → แก้เป็น start/stop เฉพาะตอน transition
- **ลดความถี่แล้ว counter ค้าง** — ถ้า `TIM2->CNT` เกิน ARR ใหม่ ต้องรอ counter วนครบก่อน (นานถึง 65 ms) → เพิ่มการ reset `CNT`
- **Encoder อ่านพลาดแล้วยัง step ต่อ** — เดิม return เฉยๆ แต่ PWM ยังเดินจากรอบก่อน → เพิ่ม `Step_Stop()` ทันที และนับพลาดครบ 50 ครั้ง (50ms) เข้า `STATE_ERROR`
- **`dt` hardcode `0.001f`** — เปลี่ยนเป็น `CONTROL_LOOP_DT` ผูกกับ `CONTROL_LOOP_HZ` จุดเดียว
- **Timeout ตายตัว 5 วินาที** — สายยาวจะ timeout ทั้งที่ยังวิ่งปกติ → เปลี่ยนเป็นคำนวณจากระยะทาง (`MOVE_TIMEOUT_BASE_MS` + ระยะ/ความเร็ว)
- **Homing ไม่มี timeout** — ถ้าสวิตช์เสียจะวนค้างตลอดไป → เพิ่ม `HOMING_TIMEOUT_MS`

### 5. ทิศทางการหมุน (DIR)

- ปัญหาแรก: โค้ดคิดว่า DIR=HIGH คือใบมีดขึ้น แต่จริงคือ**ลง** — ถ้าไม่แก้ ตอน home ใบมีดจะพุ่งชนแท่นแทนที่จะถอยขึ้นหาสวิตช์
- ปัญหาที่สอง: roller กับใบมีดทิศไม่เหมือนกันเสมอไป แต่ใช้สาย DIR เส้นเดียวกัน (PA1) จะ hardcode ค่าเดียวไม่ได้
- แก้เป็น `#define DIR_ROLLER_FORWARD` / `#define DIR_BLADE_DOWN` แยกต่อแกน ทั้ง control loop และ `Home_Blade()` อ่านค่าจากจุดเดียวกัน ป้องกันไม่ให้สองจุดขัดแย้งกันเอง
- ค่าจริงที่ยืนยันจากเจ้าของเครื่อง: **roller และใบมีดทั้งคู่ใช้ DIR=HIGH** (`GPIO_PIN_SET`) เป็นทิศทำงาน (roller ป้อนสายเข้า, ใบมีดลง)

### 6. ค่าคงที่ทางฟิสิกส์ของเครื่อง

- `DEG_PER_MM` เดิม = 3.6 (ค่าเดา ตรงกับ roller ~31.8 มม.) → แก้เป็น **3.8197** คำนวณจาก roller เส้นผ่านศูนย์กลางจริง 30 มม. (`360 / (π × 30)`) — ถ้าไม่แก้ สั่งตัด 100 มม. จะได้จริง 106 มม. (เพี้ยน 5.75%)
- เพิ่ม `PID_DEADBAND_DEG = 0.70°` และ `POS_TOLERANCE_DEG = 0.75°` คำนวณจาก `STEP_FREQ_MIN ÷ Kp` เพื่อให้ deadband ครอบคลุมโซนที่ step generator ขยับไม่ได้จริง (แก้ปัญหามอเตอร์ไหลตอนจอด)

---

## Core/Src/gpio.c และ Core/Inc/main.h

- PA10 (`Limit_Switch_Pin`) เดิมตั้งเป็น `GPIO_NOPULL` — เป็นขาลอย รับสัญญาณรบกวนได้ ทำให้ใบมีดตอน home อาจหยุดกลางทางหรือไม่หยุดเลย → แก้เป็น `GPIO_PULLUP` ต่อสวิตช์ลง GND ได้เลยไม่ต้องมีตัวต้านทานเพิ่ม
- `Blade_Limit_Pin` ใน `main.h` alias ไปที่ `Limit_Switch_Pin` (PA10) ตัวเดียวกัน — ไม่ได้เพิ่มขาใหม่ (ระหว่างทางเคยเพิ่ม PB4 ไปก่อน แต่ภายหลังยืนยันว่า PA10 คือสวิตช์ใบมีดอยู่แล้ว จึงถอด PB4 ออก)

---

## SPI-ENCODER.ioc

- ป้ายชื่อ PA10 เปลี่ยนจาก "Limit Switch" เป็น "Blade Limit Switch" ให้ตรงกับหน้าที่จริง
- ตั้ง `GPIO_PuPd=GPIO_PULLUP` ให้ตรงกับที่แก้ใน `gpio.c` เพื่อไม่ให้ค่าเพี้ยนถ้า regenerate จาก CubeMX

---

## HARDWARE.md (ไฟล์ใหม่)

คู่มือฮาร์ดแวร์ — ผังขาทั้งหมด, การตั้ง address TMC2209 ทั้ง 3 ตัว, หลักการแชร์ STEP/DIR ระหว่าง 3 มอเตอร์, ตารางทิศทางการหมุน, checklist ก่อนเปิดเครื่องครั้งแรก

## TUNING.md (ไฟล์ใหม่)

คู่มือจูนค่าแบบเขียนสำหรับมือใหม่ — วิธีตรวจสอบ/วัด `DEG_PER_MM`, วิธีหาระยะใบมีดสำหรับตัดและปอก, หลักการจูน PID (Kp/Ki/Kd) พร้อมตารางอาการ→วิธีแก้, ผลทดสอบความเร็วจากการจำลอง

## Tools/sim_control_loop.c และ Tools/README.md (ไฟล์ใหม่)

โปรแกรมจำลอง PID รันบนคอมพิวเตอร์ได้โดยไม่ต้องต่อบอร์ด ใช้ไฟล์ `PID.c` ตัวจริงจึงทดสอบสูตรเดียวกับที่ลงเครื่อง ครอบคลุม 5 กรณี: การป้อนสายถึงเป้าไหม, การนับตำแหน่งข้ามรอบ (multi-turn), ลำดับใบมีดครบ, การไหลตอนจอด (creep), และ anti-windup ตอนมอเตอร์ติดขัด — ใช้เปรียบเทียบค่า PID ก่อนเอาไปลองบนเครื่องจริง

---

## สิ่งที่ตรวจสอบแล้ว

- Build ผ่านด้วย `arm-none-eabi-gcc` 14.2 ทั้งโปรเจกต์ ไม่มี warning แม้เปิด `-Wall -Wextra`
- Simulation (`Tools/sim_control_loop.c`) ผ่านทั้ง 5 test ด้วยค่าจริงของเครื่อง (roller 30 มม., NEMA17, DEG_PER_MM=3.8197)

## สิ่งที่ยังไม่ได้ทำ / ยังเป็นความเสี่ยง

- **ยังไม่ได้ทดสอบบนฮาร์ดแวร์จริง** — ตรวจสอบได้แค่ผ่าน compiler และการจำลองบนคอมพิวเตอร์
- **ค่าระยะใบมีดยังเป็นค่าสมมติ** (`BLADE_CUT_DEG=180`, `BLADE_STRIP_DEG=120`) ต้องวัดจากเครื่องจริงตาม TUNING.md ก่อนใช้งานจริง
- **SPI ใน ISR ยังเป็น blocking call** timeout 10 ms — ปกติ transfer เร็วพอ (~15 µs) แต่ถ้า SPI ค้างจะกิน 10 รอบควบคุม
- **M2 ไม่มี encoder** ถ้าลื่นไถลไม่เท่า M1 ระบบจะไม่รู้ตัว เป็นข้อจำกัดของฮาร์ดแวร์ ไม่ใช่ซอฟต์แวร์

---

## แก้เพิ่มเติม: Step generator (ARR/CCR1)

หลังรีวิวซ้ำพบว่าวิธีเขียน `ARR` และ `CCR1` ตรงๆ ที่ใช้อยู่ **ใช้ไม่ได้จริง** มีปัญหา 3 อย่าง:

### 1. ARR กับ CCR1 มีผลคนละเวลา (ตัวหลัก)

| ทะเบียน | Preload | มีผลเมื่อไหร่ |
|---|---|---|
| `ARR` | ปิด (ค่า default ของ CubeMX) | **ทันที** |
| `CCR1` | เปิด (HAL ตั้ง `OC1PE` ให้เองเสมอ) | **update event ถัดไป** |

เขียนสองบรรทัดติดกันดูเหมือนมีผลพร้อมกัน แต่จริงๆ ห่างกัน 1 รอบ
ตอน**เร่งความเร็ว** ARR ใหม่ (เล็กลง) มีผลทันที แต่ CCR1 เก่า (ใหญ่กว่า ARR ใหม่) ยังค้างอยู่
→ compare ไม่เคยตรง → output ค้าง HIGH ทั้งรอบ → **ไม่มีขอบขาขึ้น TMC2209 ไม่นับ step**

**แก้**: เปิด ARR preload (`ARPE`) ใน `MX_TIM2_Init()` ให้ทั้งสองทะเบียนสลับพร้อมกันในฮาร์ดแวร์
ผลพลอยได้คือพัลส์ที่กำลังสร้างอยู่จบสมบูรณ์ ไม่ถูกตัดกลางคัน

### 2. โค้ดรีเซ็ต `CNT` ผิดและไม่จำเป็น

บรรทัด `if (TIM2->CNT > arr_val) TIM2->CNT = 0;` ที่เคยใส่ไว้:
- เป็น race condition (อ่าน CNT แล้วเขียน ขณะ counter เดินตลอด)
- การบังคับ CNT = 0 **ตัดพัลส์ที่กำลังสร้างอยู่ทิ้ง** ทำให้ step หายและตำแหน่งเพี้ยนสะสม
- พอเปิด ARPE แล้วไม่จำเป็นอีกต่อไป

**แก้**: ลบทิ้ง แล้วใช้ `TIM2->EGR = TIM_EGR_UG` เฉพาะตอนเริ่มจากหยุดสนิท (ตอนนั้นไม่มีพัลส์ให้ตัด และไม่มี update event มาโหลดค่าใหม่ให้)

### 3. ISR แย่งคุมมอเตอร์กับ `Home_Blade()`

`Home_Blade()` สั่ง `Select_Drive_Target(DRIVE_BLADE)` ซึ่งทำให้ ISR 1 kHz เริ่มรัน closed-loop กับใบมีดทันที **ขณะที่ `Home_Blade()` กำลังสั่ง open-loop อยู่** ทั้งคู่เขียนขา DIR และทะเบียน timer ชนกัน แถม PID ตอนนั้นอ้างอิงจุด 0 ที่ยังไม่ได้ตั้ง

**แก้**: เพิ่มธง `open_loop_active` ให้ ISR หยุดทำงานชั่วคราวระหว่าง homing ปล่อยธงคืนทุกทางออก (abort / timeout / encoder error / สำเร็จ) โดยกรณีสำเร็จปล่อยหลังตั้งจุด 0 เสร็จแล้ว เพื่อให้ ISR รับช่วงต่อโดยไม่สั่งกระโดด

**ไฟล์ที่แก้**: `Core/Src/tim.c` (เปิด ARPE), `Core/Src/main.c` (`Step_SetFrequency`, `Control_Loop_1kHz`, `Home_Blade`), `SPI-ENCODER.ioc` (ให้ตรงกับ tim.c)
